const routes = {
  admin: '/admin',
  deploy: '/deploy',
  initialize: '/initialize',
  whitelist: '/whitelist',
  add: '/add',
  settings: '/settings',
  authorize: '/authorize',
};

export default routes;
